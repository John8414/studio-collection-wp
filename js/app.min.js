/******/ (() => { // webpackBootstrap
/*!***********************!*\
  !*** ./src/js/app.js ***!
  \***********************/
$(document).ready(function () {
  // Scale zoom in image //
  var imageContainers = document.querySelectorAll('.img-scale');
  imageContainers.forEach(function (imageContainer) {
    var img = imageContainer.querySelector('img');
    var handleMouseMove = function handleMouseMove(e) {
      var rect = imageContainer.getBoundingClientRect();
      var x = e.clientX - rect.left;
      var y = e.clientY - rect.top;
      var xPercent = x / rect.width * 100;
      var yPercent = y / rect.height * 100;
      img.style.transformOrigin = "".concat(xPercent, "% ").concat(yPercent, "%");
    };
    var handleMouseLeave = function handleMouseLeave() {
      img.style.transformOrigin = 'center center';
    };
    imageContainer.addEventListener('mousemove', handleMouseMove);
    imageContainer.addEventListener('mouseleave', handleMouseLeave);
    var removeZoomEffect = function removeZoomEffect() {
      imageContainer.removeEventListener('mousemove', handleMouseMove);
      imageContainer.removeEventListener('mouseleave', handleMouseLeave);
    };
  });
  // Scale zoom in image //

  // còn dùng nhưng do trang khác ko có header nên đóng nhó
  // add search input //
  document.querySelector('.search-text').addEventListener('click', function () {
    document.querySelector('.search-header').classList.add('clicked');
  });
  $('.slick-slider').each(function () {
    $(this).slick({
      dots: false,
      infinite: false,
      speed: 500,
      slidesToShow: 4,
      slidesToScroll: 1,
      autoplay: true,
      autoplaySpeed: 2000,
      responsive: [{
        breakpoint: 1024,
        settings: {
          slidesToShow: 3,
          slidesToScroll: 1,
          infinite: true,
          dots: true
        }
      }, {
        breakpoint: 768,
        settings: {
          slidesToShow: 2,
          slidesToScroll: 1
        }
      }, {
        breakpoint: 480,
        settings: {
          slidesToShow: 1,
          slidesToScroll: 1
        }
      }]
    });
  });
  $('.prev-btn').on('click', function () {
    var targetSlider = $(this).data('slider-id');
    $('#' + targetSlider).slick('slickPrev');
  });
  $('.next-btn').on('click', function () {
    var targetSlider = $(this).data('slider-id');
    $('#' + targetSlider).slick('slickNext');
  });

  // Product outlet

  // tags
  $('.tag').each(function () {
    $(this).on('click', function () {
      $(this).toggleClass('clicked');
    });
  });
  $('.slider-show-3').each(function () {
    $(this).slick({
      dots: false,
      infinite: false,
      speed: 500,
      slidesToShow: 3,
      slidesToScroll: 1,
      autoplay: true,
      autoplaySpeed: 2000,
      responsive: [{
        breakpoint: 768,
        settings: {
          slidesToShow: 2,
          slidesToScroll: 1
        }
      }, {
        breakpoint: 480,
        settings: {
          slidesToShow: 1,
          slidesToScroll: 1
        }
      }]
    });
  });
  function toggleImage(button) {
    var img = button.querySelector('img');
    var plus = 'plus.svg';
    var minus = 'minus.svg';
    img.src = img.src.endsWith(plus) ? "images/".concat(minus) : "images/".concat(plus);
  }

  // toggle up and down

  function toggleImageUpDown(button) {
    var img = button.querySelector('img');
    var up = 'arr-down-thin.svg';
    var down = 'arr-up-thin.svg';
    img.src = img.src.endsWith(up) ? "images/".concat(down) : "images/".concat(up);
  }

  //image picker

  document.querySelectorAll('.image-picker').forEach(function (picker) {
    var mainImage = picker.querySelector('.main-image');
    var thumbnails = picker.querySelectorAll('.thumbnails img');
    thumbnails.forEach(function (thumbnail) {
      thumbnail.addEventListener('click', function () {
        var newSrc = thumbnail.getAttribute('src');
        mainImage.src = newSrc;
      });
    });
  });

  /**
  * back to top
  */
  $("#backToTop").on("click", function () {
    $("body,html").animate({
      scrollTop: 0
    }, "slow");
  });
  $(window).scroll(function () {
    if ($(this).scrollTop() > 60) {
      $(".to-top").addClass("d-block");
    } else {
      $(".to-top").removeClass("d-block");
    }
  });
});
/******/ })()
;
//# sourceMappingURL=app.min.js.map